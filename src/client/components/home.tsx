import * as React from 'react';
import { useState, useEffect } from 'react';
import { Book } from '../../common/types';
import { get } from '../api';

export const Home = () => {

    return (
        <div>
            Home
        </div>
    )
}